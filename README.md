# aitool-frontend
AI tools recommendation web app built with React and AWS serverless backend.  Supports natural language queries, parses user intent into structured tags,  and retrieves relevant tools from a curated database with scoring and fallback logic.
